-- NOT NULL:A not null constraint ensure that a table column cannot be left empty..
-- When a column is defined as not null, then that column
-- becomes a mandatory column. It implies that a value must be entered into the 
-- column if the record is to be accepted for storage in the table.


-- Syntax: <Column Name> <data type>(<size>) NOT NULL ;
name varchar(22) not null;